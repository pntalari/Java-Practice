package com.zipcoder.quiz5;

import java.util.List;

public class RearrangeCharacters {
    /**
     * Given a word, return all the different order of the letters.
     * For example, given the input "cat", the method should return a
     * list with "cat", "cta", "act", "atc", "tac", "tca". The order
     * doesn't matter.
     *
     * @param input
     * @return Return a list of all rearrangement of the characters
     */
    public List<String> getWords(String input) {
        return null;
    }
}
